# app

Placeholder for a future Flask web interface for the NFL DFS optimizer -
not built yet. Every workflow currently runs through direct calls into
`models/` and `data/`; see the root README's "Core workflows" section.
