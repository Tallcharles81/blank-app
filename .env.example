PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=nfl_dfs
PGUSER=nfl_dfs
PGPASSWORD=changeme

# Real Anthropic API key - required for data/news_scanner.py and
# models/lineup_anomaly_review.py (the AI-powered news scan / anomaly review
# layers). Get a real key at https://console.anthropic.com/ - never commit
# a real value here, this file only ever holds a placeholder.
ANTHROPIC_API_KEY=changeme
